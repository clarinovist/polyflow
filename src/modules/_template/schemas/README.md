# schemas/

Zod schemas and DTO validation for this module.

Use schemas for input shape, required fields, primitive constraints, and safe parsing.
Domain invariants that require database state or workflow context belong in services/policies.
