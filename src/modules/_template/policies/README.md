# policies/

Permission and domain policy checks.

Policies should be deterministic, reusable, and easy to test.

Examples:

- can transition status
- can post journal in period
- can mutate stock at location
- can run tenant write operation
