export const PRODUCTION_ORDERS_LIST_DEFAULT_PAGE_SIZE = 25;

/**
 * PerformanceMetric.route value for getProductionOrdersList — shared between
 * the writer (production-orders.ts) and the admin.polyflow.uk reader
 * (performance-metrics.ts) so the string literal exists in one place.
 */
export const PRODUCTION_ORDERS_LIST_ROUTE = 'production-orders-list';
