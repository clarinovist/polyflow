-- Prisma client rename only: Unit.PACK @map("PCS") → Unit.PCS
-- PostgreSQL enum label was already "PCS"; no data rewrite required.
-- Etiket / piece items already stored as primaryUnit = 'PCS'.

SELECT 1;
